
export type NewsSource = {
    id: string
    name: string
    url: string
    country: string
}

export type NewsFeed = {
    url: string
    language: string
}

export type Locale = {
    lang: string
    country: string
}
